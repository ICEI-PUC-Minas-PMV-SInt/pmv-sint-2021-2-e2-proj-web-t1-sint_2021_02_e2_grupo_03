import styled from 'styled-components';

const SalvarButton = styled.button`
  width: 128px;
  height: 58px;
  background: rgba(22, 165, 62, 0.7);
  border-radius: 5px;
`;

export default SalvarButton;
